import { ObjetArrayToStringPipe } from './objet-array-to-string.pipe';

describe('ObjetArrayToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjetArrayToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
